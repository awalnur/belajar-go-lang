package main

import "fmt"

func main() {
	var names string
	names = "names"
	fmt.Println(names)
	var name = "asdads"
	//name = "namaasd"
	fmt.Println(name)

	atest := "test"

	fmt.Println(atest)

	var (
		first = "first"
		last  = "alast"
	)

	fmt.Println(first, last)
}
